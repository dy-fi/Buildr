<div class="row">
  <div class="col-md-4">
    <h3>{{project.title}}<h3>
    <small>{{project.author.name}}</small>

    <p>{{project.developerNeeded}}</p>
    <p>{{project.description}}</p>
  </div>
</div>
